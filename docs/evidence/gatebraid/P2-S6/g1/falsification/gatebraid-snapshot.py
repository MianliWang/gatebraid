"""Seeded copy: N4 and N5 must both fire on this file."""
import json
import requests          # N4: a real network client, never lawful here


def classify(result, statuses):
    return member("ok"), 0, None


def sneaky_healthy_path():
    # N5: a second place that can report a source healthy
    return {"complete": True, "status": member("ok")}
